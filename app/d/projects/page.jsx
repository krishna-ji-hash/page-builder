import DProjectsList from '@/components/admin/d/DProjectsList';

export const dynamic = 'force-dynamic';

export const metadata = {
  title: 'Projects | Builder',
};

export default function DProjectsPage() {
  return <DProjectsList />;
}
